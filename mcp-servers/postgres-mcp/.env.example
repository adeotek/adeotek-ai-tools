# PostgreSQL MCP Server Environment Variables (Read-Only Mode)
# Copy this file to .env and fill in your values

# PostgreSQL Configuration
# NOTE: Connection parameters are no longer configured via environment variables
# Instead, initialize the MCP server via POST /mcp/initialize with server connection parameters
# Database names are then specified per tool call

# Security Settings
Security__EnableRateLimiting=false
Security__RequestsPerMinute=60
Security__MaxRowsPerQuery=1000
Security__MaxQueryExecutionSeconds=30

# Logging
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft__AspNetCore=Warning
Logging__LogQueries=true
Logging__LogResults=false

# ASP.NET Core
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://+:5000
